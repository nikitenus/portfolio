import {Icon} from '../icon/Icon.tsx';
import * as React from "react";

export const Logo: React.FC = () => {
    return (
        <a href="">
            <Icon iconId={'code'}/>
        </a>
    );
};